package com.imageauth.verifier.screens

import androidx.compose.ui.graphics.Color

// ─────────── Web App Matching Color Palette ───────────
val AppBackground   = Color(0xFFF9FAFB)  // gray-50
val CardBackground  = Color(0xFFFFFFFF)  // pure white

// ─────────── Header & Primary Gradients ─────────────
val GradientBlueStart = Color(0xFF2563EB)  // blue-600
val GradientPurpleEnd = Color(0xFF7E22CE)  // purple-700
val UploadBlueStart   = Color(0xFF2563EB)  // blue-600
val UploadPurpleEnd   = Color(0xFF9333EA)  // purple-600

// ─────────── Dark Mode Fallback Tokens ──────────────
val DarkBg          = Color(0xFF0F172A)  // slate-900
val DarkBgDeep      = Color(0xFF020617)  // slate-950

// ─────────── Brand Accents ────────────
val PrimaryAccent   = Color(0xFF2563EB)  // blue-600
val SecondaryAccent = Color(0xFF7E22CE)  // purple-700

// ─────────── Card / Surface ───────────
val CardBg          = Color(0xFFFFFFFF)  // white
val CardBgLight     = Color(0xFFF3F4F6)  // gray-100

// ─────────── Text Colors ──────────────
val TextMain        = Color(0xFF111827)  // gray-900
val TextMuted       = Color(0xFF4B5563)  // gray-600
val TextDim         = Color(0xFF9CA3AF)  // gray-400
val TextBlueLight   = Color(0xFFDBEAFE)  // blue-100

// ─────────── Status Colors ───────────
val GreenAuthentic  = Color(0xFF22C55E)  // green-500
val GreenSurface    = Color(0xFFF0FDF4)  // green-50
val GreenBorder     = Color(0xFFBBF7D0)  // green-200
val RedFake         = Color(0xFFEF4444)  // red-500
val RedSurface      = Color(0xFFFEF2F2)  // red-50
val OrangeUncertain = Color(0xFFF59E0B)  // amber-500
val OrangeSurface   = Color(0xFFFFFBEB)  // amber-50
val BlueInfo        = Color(0xFF0284C7)  // sky-600
val BlueBoxBg       = Color(0xFFE0F2FE)  // sky-100
val PurpleBoxBg     = Color(0xFFF3E8FF)  // purple-100

// ─────────── Divider ──────────────────
val DividerColor    = Color(0xFFE5E7EB)  // gray-200
